# Git workflow

- Commit automatically after each logical change/task is completed — don't wait to be asked.
- Never push without asking first. Always confirm with the user before running `git push`, even if a commit was just made automatically.
